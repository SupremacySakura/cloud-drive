# 决策记录 - 后端安全加固
- 采用环境变量驱动 JWT 密钥和 CORS 配置，避免代码层面的敏感信息暴露。
- 使用 gin-contrib/cors 作为官方中间件，简化跨域处理并提升兼容性。
- 采用内存实现的双级令牌桶限流，避免外部依赖（如 Redis），以便快速验证与迭代。
- 路由注册阶段应用中间件，确保所有路由统一受控。
- .env.example 增加 CORS_ALLOWED_ORIGINS，便于开发环境快速配置。
