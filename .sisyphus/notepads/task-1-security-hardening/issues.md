# 问题记录 - 后端安全加固
- 需要确认 JWT_SECRET 的环境变量在各部署场景中的注入方式（Docker Compose、Kubernetes 等）。
- CORS 允许源的配置需要根据前端域名在不同环境中调整，当前实现依赖 CORS_ALLOWED_ORIGINS 环境变量，缺省情况下允许所有来源，这在生产环境不安全，需严格配置。
- 令牌桶限流的内存实现可能在极端高并发场景下内存占用较高，需要监控与清理策略。
- 尚未添加对多个登录端点的细粒度限流策略（若有多种登录入口，需统一处理）。
